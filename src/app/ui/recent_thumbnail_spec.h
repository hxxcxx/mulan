#pragma once

namespace recent_thumbnail {

inline constexpr int kDisplayWidth = 180;
inline constexpr int kDisplayHeight = 129;

// Higher-resolution source with the same ~1.4:1 aspect ratio as the startup-page tile.
inline constexpr int kCaptureWidth = 560;
inline constexpr int kCaptureHeight = 400;

}  // namespace recent_thumbnail
